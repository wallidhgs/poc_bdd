namespace Steps
{
    // Describes how the pages are named in the feature files
    public class PageNames {
        public static readonly string DASHBOARD = "dashboard";
        public static readonly string LOGIN = "login";
    }
    // Page partial path in webapp
    public class PagePath {
        public static readonly string DASHBOARD = "/";
        public static readonly string LOGIN = "/login";
    }
    // Describes how the notifications are named in the feature files
    public class PopupTypes {
        public static readonly string ERROR = "error";
    }
}
